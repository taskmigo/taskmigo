services:
  postgres:
    image: postgres:18.4-alpine
    environment:
      POSTGRES_DB: taskmigo
      POSTGRES_USER: taskmigo
      POSTGRES_PASSWORD: taskmigo
    ports:
      - "5432:5432"
    volumes:
      - taskmigo-postgres:/var/lib/postgresql

volumes:
  taskmigo-postgres:
