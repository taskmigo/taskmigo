/// Publishes managed provisioning result and error contracts.
///
/// Provisioning use cases are exposed separately through the `provisioning-input` named interface.
@NamedInterface("provisioning")
@NullMarked
package io.taskmigo.identity.provisioning;

import org.jspecify.annotations.NullMarked;
import org.springframework.modulith.NamedInterface;
